import * as React from "react";
import { Component, } from "react";
import "./App.css";

export class App extends Component {
    readonly state = {
        isShow: true,
    }
    render() {
        return (
            <div >
            </div>
        );
    }
}
